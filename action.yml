name: 'AIknowledge Notify'
description: 'Notification action for AIknowledge repository'
author: 'mswnlz'
inputs:
  message:
    description: 'The message to print'
    required: false
    default: 'Hello from AIknowledge Action!'
runs:
  using: 'node20'
  main: 'dist/index.js'
